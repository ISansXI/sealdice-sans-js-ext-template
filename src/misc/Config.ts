const Config = {
    // 扩展的名字，不要和其他扩展重复
    "ext_name": "sans-js-ext-template",

    // 作者名
    "author": "Author Name",

    // 版本
    "version": "1.0.0",

    // 指令唤醒词,也就是一层指令
    "awake_words": [
        "sans-template"
    ],

    // 是否在非指令时也运行？
    "no_command_enabled": false,

    // [确认后不要改动] 该项目的Data仓库名
    "data_namespace": "sans-js-ext-template-data"
}

export {
    Config
}